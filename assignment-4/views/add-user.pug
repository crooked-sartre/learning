extends layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/forms.css")

block content
    main
        form.product-form(action="/add-user", method="POST")
            .form-control
                label(for="Add User")
                input(type="text", name="username")#title
            
            button(class="btn", type="submit") Add User
